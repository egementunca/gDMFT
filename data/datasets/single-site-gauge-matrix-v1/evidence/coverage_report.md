# Single-Site Gauge Matrix — Coverage Report

Generated by `scripts/gauge_matrix/build_grids.py` (deterministic; no solver run).
Keys are reduced-unit `(U/D, T/D)`. Coverage is reported **separately** for each
evidence type — bare_native, R_converted_from_bare (deterministic partner),
R_reoptimized_from_bare, R_native_continuation — never merged.

## gem benchmark keys ⊆ dense Mg=3 grid (acceptance criterion 10)

| set | gem keys | keys outside dense grid |
|---|---|---|
| bethe_B1 | 884 | 0 |
| bethe_B3 | 884 | 0 |
| square_B1 | 400 | 0 |
| square_B3 | 400 | 0 |

Every live gem `(U/D,T/D)` key lies on the corresponding dense Mg=3 grid.

## Coverage by cell and evidence type

| lattice | Mg | grid keys | bare covered | bare missing | R-conv == bare | R-native T/D | R-reopt T/D |
|---|---|---|---|---|---|---|---|
| bethe | 1 | 884 | 225 | 659 | True | 0 | 0 |
| bethe | 3 | 884 | 884 | 0 | True | 3 | 0 |
| square | 1 | 400 | 70 | 330 | True | 0 | 0 |
| square | 3 | 400 | 400 | 0 | True | 0 | 10 |

`R-conv == bare` True confirms **every bare root has exactly one R_converted_from_bare partner** (acceptance criterion 3): the converted evidence covers precisely the bare key set.

## Missing independent R-native scans (Required-Gauge-Work step 5)

- **bethe Mg=3**: R-native ladders present at T/D=[0.005, 0.025, 0.1]; **14 missing** T/D ladders: [0.001, 0.002, 0.003, 0.004, 0.0065, 0.008, 0.01, 0.015, 0.02, 0.03, 0.05, 0.07, 0.15, 0.2]
- **square Mg=3**: R-native ladders present at T/D=[]; **10 missing** T/D ladders: [0.001, 0.002, 0.003, 0.004, 0.005, 0.006, 0.008, 0.01, 0.015, 0.02]

These are only needed for a *full independent solver-route parity* claim; the deterministic `R_converted_from_bare` partners already supply the complete gauge representation for every bare root.

## Missing Mg=1 benchmark keys (acceptance criterion 11 — enumerated, not interpolated)

### bethe Mg=1 — 659 of 884 benchmark keys absent

Covered: 225. The existing Mg=1 campaign scans only specific arms/temperatures, so most dense-grid keys are absent. Full enumeration in `coverage_matrix.csv` (rows with `lattice=bethe, M_g=1, has_bare_native=0`).

Missing keys span T/D ladders: [0.001, 0.002, 0.003, 0.004, 0.005, 0.0065, 0.008, 0.01, 0.015, 0.02, 0.025, 0.03, 0.05, 0.07, 0.1, 0.15, 0.2]

### square Mg=1 — 330 of 400 benchmark keys absent

Covered: 70. The existing Mg=1 campaign scans only specific arms/temperatures, so most dense-grid keys are absent. Full enumeration in `coverage_matrix.csv` (rows with `lattice=square, M_g=1, has_bare_native=0`).

Missing keys span T/D ladders: [0.001, 0.002, 0.003, 0.004, 0.005, 0.006, 0.008, 0.01, 0.015, 0.02]
